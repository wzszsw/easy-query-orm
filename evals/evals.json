{
  "skill_name": "easy-query",
  "design": {
    "version": 1,
    "goal": "Measure whether the skill routes to verified easy-query APIs without falling back to generic ORM habits, manual SQL, or post-query in-memory workarounds.",
    "dimensions": [
      {
        "id": "setup-and-generation",
        "purpose": "Proxy generation, missing proxy troubleshooting, Kotlin KSP vs KAPT, starter setup, and Spring integration.",
        "eval_ids": [
          0,
          1,
          2,
          18
        ]
      },
      {
        "id": "core-query-and-write",
        "purpose": "CRUD, dynamic paging filters, transactions, and tests grounded in easy-query APIs.",
        "eval_ids": [
          3,
          4,
          5,
          6
        ]
      },
      {
        "id": "implicit-core",
        "purpose": "Implicit group, partition, joining, boolean relation projection, and structured relation loading.",
        "eval_ids": [
          7,
          8,
          17
        ]
      },
      {
        "id": "implicit-controls",
        "purpose": "any/all/none/notEmptyAll, flatElement, subQueryConfigure, filter/configure/mode, join-on filtering, relation distinct, predicate-to-boolean projection, and permission-tree routing.",
        "eval_ids": [
          9,
          10,
          11,
          13,
          14,
          15,
          16
        ]
      },
      {
        "id": "projection-and-vo-assembly",
        "purpose": "Proxy VO relation projection without selectAutoInclude.",
        "eval_ids": [
          12
        ]
      },
      {
        "id": "ap-analytics",
        "purpose": "Explicit groupBy/having, conditional aggregates, implicit group joins for metrics, CTE/window analytics, and UNION-based report shaping.",
        "eval_ids": [
          19,
          20,
          21
        ]
      },
      {
        "id": "form-query-conditions",
        "purpose": "whereObject, @EasyWhereCondition, range/in/notIn, MULTI_OR, relation-path filters, and EasySearch/@EasyCond boundaries.",
        "eval_ids": [
          22,
          23
        ]
      },
      {
        "id": "conversion-and-type-handler",
        "purpose": "ValueConverter, ValueAutoConverter, @Enumerated, and JdbcTypeHandler boundaries plus jsonb-style combined handling.",
        "eval_ids": [
          24,
          25
        ]
      },
      {
        "id": "interceptors",
        "purpose": "EntityInterceptor, PredicateFilterInterceptor, UpdateSetInterceptor, UpdateEntityColumnInterceptor, ProtectedInterceptor, and use/noInterceptor control.",
        "eval_ids": [
          26,
          27
        ]
      },
      {
        "id": "logic-delete",
        "purpose": "@LogicDelete, built-in/custom strategies, query-time toggles, table-level disable, and physical-delete escape hatches.",
        "eval_ids": [
          28,
          29
        ]
      },
      {
        "id": "spring-boot-starter",
        "purpose": "Starter auto-configuration topology, property semantics, collected Spring beans, StarterConfigurer, tracking AOP, and multi-datasource/manual-build boundaries.",
        "eval_ids": [
          30,
          31
        ]
      },
      {
        "id": "explicit-subquery",
        "purpose": "Explicit where/select/from/join subqueries, context-aware subQueryable, derived tables, grouped subquery join, and CTE promotion boundaries.",
        "eval_ids": [
          32,
          33
        ]
      },
      {
        "id": "write-mutation-depth",
        "purpose": "Insert/upsert, object update vs expression update, delete safety, SQL strategy, map writes, batch semantics, and diff-update tracking boundaries.",
        "eval_ids": [
          34,
          35,
          36,
          37
        ]
      },
      {
        "id": "savable-aggregate",
        "purpose": "Savable transaction/tracking prerequisites, savePath/root controls, relation ownership/cascade rules, and child-key safety.",
        "eval_ids": [
          38,
          39
        ]
      }
    ],
    "pass_policy": {
      "p0_must_pass_eval_ids": [
        1,
        7,
        8,
        10,
        11,
        18,
        19,
        20,
        22,
        24,
        26,
        28,
        30,
        32,
        34,
        36,
        37,
        38
      ],
      "p1_should_pass_eval_ids": [
        9,
        12,
        13,
        14,
        15,
        16,
        17,
        21,
        23,
        25,
        27,
        29,
        31,
        33,
        35,
        39
      ],
      "p2_regression_guard_eval_ids": [
        0,
        2,
        3,
        4,
        5,
        6
      ]
    },
    "benchmark_focus_eval_ids": [
      1,
      7,
      8,
      10,
      12,
      17,
      19,
      20,
      22,
      24,
      26,
      28,
      30,
      32,
      34,
      36,
      37,
      38
    ]
  },
  "evals": [
    {
      "id": 0,
      "name": "kotlin-entity-and-query",
      "prompt": "我用 Kotlin 和 easy-query 这个 ORM。帮我定义一个 Topic 实体（表 t_topic，字段 id 主键 String、stars Int、title String、createTime），然后写一段查询：找出 stars 大于 100 的、按 createTime 倒序的前若干条，返回 List。给我能直接编译的 Kotlin 代码。",
      "expected_output": "A Kotlin entity annotated @Table/@EntityProxy implementing ProxyEntityAvailable<Topic, TopicProxy>, plus a query using easyEntityQuery.queryable(Topic::class.java).where { it.stars().gt(100) }.orderBy { it.createTime().desc() }.toList(). No KAPT, no invented API.",
      "files": []
    },
    {
      "id": 1,
      "name": "kotlin-ksp-setup",
      "prompt": "我新建了一个 Kotlin Gradle 项目想接入 easy-query，连 MySQL。帮我写 build.gradle.kts 的依赖和插件配置，以及 main 里初始化 EasyEntityQuery 的代码。我之前用别的库是 kapt，这个也用 kapt 吗？",
      "expected_output": "Gradle KSP setup (com.google.devtools.ksp plugin + ksp(\"com.easy-query:sql-ksp-processor\")), the kotlin.srcDir(\"build/generated/ksp/main/kotlin\") line, EasyQueryBootstrapper init with MySQLDatabaseConfiguration + DefaultEasyEntityQuery. Must explicitly say to use KSP, NOT kapt.",
      "files": []
    },
    {
      "id": 2,
      "name": "java-spring-crud-service",
      "prompt": "Spring Boot 项目，用的 easy-query。帮我写一个 UserService，包含：按手机号查唯一用户、新增用户、按 id 更新用户名。实体叫 SysUser（表 t_sys_user，字段 id 主键、username、phone）。顺便告诉我 application.yml 要配什么。",
      "expected_output": "A @Service injecting EasyEntityQuery, using singleOrNull() for the unique phone lookup, insertable(...).executeRows(), updatable(SysUser.class).setColumns(...).where(id).executeRows(). application.yml with `easy-query.database`, `name-conversion`, and other relevant starter keys; it may mention `easy-query.enable` explicitly, but it must not claim that current starter source requires `enable=true` to load by default. No JPA/MyBatis.",
      "files": []
    },
    {
      "id": 3,
      "name": "dynamic-filter-pagination",
      "prompt": "用 easy-query 写一个后台分页查询接口逻辑（Java）：根据可选的 name(模糊) 和 可选的状态 status、可选的创建时间区间过滤订单 Order，分页返回。要注意空参数不要拼进条件，分页要稳定。",
      "expected_output": "Uses gated conditions (.like(condition, value) / where(condition, lambda)) so empty params are skipped, toPageResult(pageIndex,pageSize) with orderBy plus a stable tiebreaker (e.g. id), reads getData()/getTotal(). No string concatenation of SQL.",
      "files": []
    },
    {
      "id": 4,
      "name": "transactional-multi-write",
      "prompt": "easy-query，纯 Java（没有 Spring）。下单逻辑：插入一条 Order，同时把对应 Account 的 balance 扣减订单金额，必须在一个事务里，余额不足或扣款影响行数不对就回滚。帮我写出来。",
      "expected_output": "try (Transaction tx = easyEntityQuery.beginTransaction()) { insertable(order).executeRows(); updatable(Account.class).setColumns(a -> a.balance().decrement(amount)).where(...).executeRows(1, msg); tx.commit(); }. Auto-rollback on exception. Uses decrement and a row-count assertion.",
      "files": []
    },
    {
      "id": 5,
      "name": "write-unit-test",
      "prompt": "帮我给一段 easy-query 的查询代码写单元测试，不想依赖外部数据库。实体是 Topic(t_topic: id, stars, title, createTime)。测试 stars>100 的过滤能查到正确数据。用 JUnit。",
      "expected_output": "H2 in-memory setup (H2DatabaseConfiguration, jdbc:h2:mem with DB_CLOSE_DELAY=-1), code-first syncTableCommand(...).executeWithTransaction(arg -> arg.commit()), seed data, then a behavior assertion on the query result. Optionally mentions .toSQL() approach.",
      "files": []
    },
    {
      "id": 6,
      "name": "relation-query-include",
      "prompt": "easy-query 里，一个 SysUser 有多张 BankCard（一对多），我想查用户的同时把他的银行卡一起加载出来，避免 N+1。怎么定义关系、怎么查？给 Java 代码。",
      "expected_output": "Entity field with @Navigate(value = RelationTypeEnum.OneToMany, selfProperty=..., targetProperty=...), and a query using .include(user -> user.bankCards()).toList(). Explains it avoids N+1. No manual loop querying.",
      "files": []
    },
    {
      "id": 7,
      "name": "implicit-group-partition-joining",
      "prompt": "用 easy-query 写一个 Java 查询：返回每个用户的 id，以及他按 openTime 升序取前 2 张银行卡后的 type 拼接字符串；同时再返回一个布尔值表示这个用户是否有任意银行卡。要求使用 easy-query 的隐式关系能力，不要手写 SQL，也不要先查出来再内存处理。",
      "expected_output": "Uses relation-driven implicit APIs, ideally .subQueryToGroupJoin(user -> user.bankCards()) or verified ALL_SUB_QUERY_GROUP_JOIN behavior when combining multiple to-many operators, then orderBy(...).elements(0,1).joining(card -> card.type(), \",\") plus anyValue() or noneValue(). Must preserve deterministic order and avoid invented SQL or stream post-processing.",
      "files": []
    },
    {
      "id": 8,
      "name": "select-auto-include-extra-configure",
      "prompt": "我想用 easy-query 的 selectAutoInclude 返回一个 UserDTO，里面有 roles 列表。role 这一层要额外过滤 name like 管理员，并额外投影 top3Menus 和 menuCount。顺便回答：如果我先写 include(...) 再 selectAutoInclude(...)，到底谁生效？给我 Java 示例。",
      "expected_output": "Uses DTO/VO result type rather than entity return type, and a nested static EXTRA_AUTO_INCLUDE_CONFIGURE = RoleProxy.TABLE.EXTRA_AUTO_INCLUDE_CONFIGURE().where(...).select(...). Optionally configures deeper implicit relation expressions. Must explicitly state that handwritten include(...) takes precedence over auto include when both are present.",
      "files": []
    },
    {
      "id": 9,
      "name": "flat-element-and-select-boundary",
      "prompt": "easy-query 里我想按隐式关系把 User 的 roles 展开，再把 roles 的 menus 继续展开，只返回最终菜单 path 列表。给我 Java 代码，并说明为什么不要把 flatElement 写进 select(...)。",
      "expected_output": "Uses toList(x -> x.roles().flatElement().menus().flatElement().path()) or another verified flatElement-in-toList pattern. Must explicitly avoid select(...flatElement...) and explain that proxy source disallows flatElement inside select, recommending toList(...) for flattened relation rows.",
      "files": []
    },
    {
      "id": 10,
      "name": "subquery-configure-filter-mode",
      "prompt": "帮我用 easy-query 写一个隐式关系查询：User 的 bankCards 这条关系统一先限制 code='1'，然后判断是否存在 type='11' 和 type='22'；另外给一个变体，强制 roles 这条关系用 GROUP_JOIN 模式去判断是否有 menu.route='/admin'。给 Java 代码。",
      "expected_output": "Uses subQueryConfigure(user -> user.bankCards(), q -> q.where(card -> card.code().eq(\"1\"))) before multiple any(...) calls, and a separate example using roles().mode(SubQueryModeEnum.GROUP_JOIN) with roles().flatElement().menus().any(...). Must not replace these with manual joins or post-query filtering.",
      "files": []
    },
    {
      "id": 11,
      "name": "to-one-filter-on-join",
      "prompt": "easy-query 里我想查 BankCard，但只联到 name 包含“工商银行”的 Bank；要求保持隐式关系写法，不要手写 join。请给 Java 代码，并说明这个 filter 条件落在 join on 还是 where。",
      "expected_output": "Uses bankCard.bank().filter(bank -> bank.name().contains(\"工商银行\")) inside where(...) and explicitly explains that for relation tables this filter is pushed into JOIN ON rather than ordinary WHERE. Must not rewrite to manual leftJoin/innerJoin.",
      "files": []
    },
    {
      "id": 12,
      "name": "proxy-relation-set-projection",
      "prompt": "我不用 selectAutoInclude，想直接 select 一个自定义 VOProxy，把用户的 bankCards 投影成 cards 子列表，里面 type 来自 card.code，code 来自 card.bank.name。请给 easy-query Java 示例。",
      "expected_output": "Uses select(user -> new MyUserVOProxy().vo1().set(...).cards().set(user.bankCards(), (self, target) -> { self.type().set(target.code()); self.code().set(target.bank().name()); })) and treats it as a valid relation-driven projection pattern rather than switching to selectAutoInclude or manual post-query mapping.",
      "files": []
    },
    {
      "id": 13,
      "name": "relation-configure-alias-and-logic-delete",
      "prompt": "给我两个 easy-query Java 片段。第一个：User 的 roles 这条隐式关系在子查询里起别名 myRole，再判断是否存在 name like 查询的角色名 且 createTime 在区间内的 role。第二个：User 的 bankCards 这条关系在隐式聚合前关闭逻辑删除，再对 type='123' 的 code 求和并比较。不要改成手写 join。",
      "expected_output": "First snippet uses roles().configure(r -> r.asAlias(\"myRole\")).any(role -> ...). Second snippet uses bankCards().configure(q -> q.disableLogicDelete()).where(card -> card.type().eq(\"123\")).sum(card -> card.code().toNumber(Integer.class)).eq(...). Must keep the implicit relation form instead of rewriting to explicit joins.",
      "files": []
    },
    {
      "id": 14,
      "name": "relation-distinct-aggregate",
      "prompt": "easy-query 里我想写一个隐式关系聚合：按 User 查询，并返回 books 这条关系去重后的 author.age 求和。请给 Java 代码，要求 distinct 发生在关系子查询内部，而不是外层 select distinct。",
      "expected_output": "Uses a relation chain like user.books().distinct().sum(book -> book.author().age()) in select/selectColumn and explicitly keeps distinct inside the implicit relation aggregate path instead of rewriting to an outer select distinct.",
      "files": []
    },
    {
      "id": 15,
      "name": "permission-menu-tree-via-implicit-path",
      "prompt": "我有 SysUser -> roles -> menus 的导航关系，想查某个 userId 的菜单树。不要手写 SysUserRole / SysRoleMenu 中间表查询，优先用 easy-query 的隐式关系和 flatElement；如果只分配了叶子菜单，还要把祖先节点补齐。给 Java 示例，并说明什么时候才退回中间表查询。",
      "expected_output": "Prefers relation-driven filtering over explicit junction-table subqueries. It must explain the tree-CTE boundary correctly: where(...).asTreeCTE() filters only seed rows, recursive filtering needs setChildFilter(...), and a true menu-tree root rule must be expressed explicitly (for example parentId is null or parentId = 0, depending on the project) rather than assumed. For a tree answer, first express the real root predicate. If parent/root menus are also assigned, keep the permission rule directly on the `SysMenu` relation path in both the anchor and the recursive child filter, for example with menu.roles().flatElement().users().flatElement().id().eq(userId). If only leaf menus are assigned, use asTreeCTE(op -> { op.setUp(true); op.setUnionAll(false); }) on the matched menus to collect ancestor ids, then rebuild the final tree from explicit root rows plus setChildFilter(child -> child.id().in(...)). Must not teach a default 'query ids first, then build tree' template. Explicitly says SysUserRole / SysRoleMenu querying is only a fallback when @Navigate metadata/path is missing.",
      "files": []
    },
    {
      "id": 16,
      "name": "quantifier-relation-predicates",
      "prompt": "帮我用 easy-query 写一个 Java 查询：按 User 查，要求至少有一张 type='储蓄卡' 的银行卡，并且这些储蓄卡的 code 必须全部以 33123 开头；另外还要求不存在任何 type='禁用卡' 的银行卡。不要手写 join。顺便说明 all(...) 和 notEmptyAll(...) 在空关系上的区别。",
      "expected_output": "Uses relation-driven quantifier predicates rather than manual joins, ideally bankCards().where(card -> card.type().eq(\"储蓄卡\")).notEmptyAll(card -> card.code().startsWith(\"33123\")) plus bankCards().none(card -> card.type().eq(\"禁用卡\")) or an equivalent verified none(...) shape. Must explicitly explain that all(...) can vacuously pass on an empty relation slice, while notEmptyAll(...) additionally requires at least one related row.",
      "files": []
    },
    {
      "id": 17,
      "name": "implicit-partition-grouping-first-child",
      "prompt": "easy-query 里我想按 User 查询，筛出“第一张开户银行卡所属银行是工商银行”的用户。要求保持隐式关系写法，不要手写 SQL 或窗口函数。给 Java 代码，并说明这类写法为什么属于 partition by / 分组后取第一条 的隐式能力。",
      "expected_output": "Uses a ranked-child relation path such as user.bankCards().orderBy(x -> x.openTime().asc()).first().bank().name().eq(\"工商银行\") inside where(...). Must describe this as implicit partition / ranked-child behavior (partition by the parent key, then take the first child after ordering) rather than rewriting it to manual SQL/window code or an explicit join workaround.",
      "files": []
    },
    {
      "id": 18,
      "name": "missing-proxy-troubleshooting",
      "prompt": "我的 easy-query 项目里一直提示 `package com.foo.entity.proxy does not exist`，`UserProxy` 没生成。项目是 Java Maven，多模块，已经引了 `sql-processor`。另外 `maven-compiler-plugin` 里还配过 `annotationProcessorPaths`，本地 JDK 21，Lombok 比较老。不要直接给我一份新 pom，我要的是一套排查顺序和原因判断。",
      "expected_output": "Must diagnose by layer instead of jumping to a pom snippet: identify Java APT path, explain that `@EntityProxy` is the generation trigger and `ProxyEntityAvailable` is not the switch, call out that existing `annotationProcessorPaths` can hide easy-query unless `sql-processor` is explicitly listed there, require checking the module that actually compiles the entities, require checking generated output under target/generated-sources/annotations, and explicitly say the visible missing-proxy error may be secondary to the first real processor/compile failure such as old Lombok on JDK 21 (`javacTree`/`JCImport`/`NoSuchFieldError`).",
      "files": []
    },
    {
      "id": 19,
      "name": "explicit-group-having-conditional-aggregate",
      "prompt": "帮我用 easy-query 写一个 Java 报表查询：按 companyId 分组统计用户数、近30天新增用户数、最大生日、平均年龄，只保留用户数大于 10 的公司。不要手写 SQL，也不要用 Java Stream 再分组。",
      "expected_output": "Uses explicit group-aware DSL: groupBy(user -> GroupKeys.of(user.companyId())), grouped projection via key1()/group.groupTable(), conditional aggregate filter syntax such as count().filter(() -> ...), and aggregate-level having(...) instead of post-query regrouping or manual SQL.",
      "files": []
    },
    {
      "id": 20,
      "name": "cte-window-topn-analytics",
      "prompt": "我想用 easy-query 做一个分析查询：先按 bankId + uid 聚合出每个用户在该银行的卡数，再给每个 bankId 内的用户按卡数倒序打排名，只保留每个 bankId 排名第 3 的用户，最后再回表查这些用户。要求用 easy-query 的 CTE / 窗口能力，不要手写窗口 SQL。",
      "expected_output": "Uses a grouped stage plus a window stage in easy-query DSL, such as GroupKeys.of(...) with rowNumberOver().partitionBy(...).orderByDescending(...), then CTE/view-style filtering on the rank column before joining or querying back users. Must keep deterministic partition ordering and avoid raw SQL strings.",
      "files": []
    },
    {
      "id": 21,
      "name": "union-shaped-report-query",
      "prompt": "有两份 easy-query 数据源分支：一份来自 Topic，一份来自 Blog，我想统一投影成同一个报表行模型，然后 union all 之后再按 id 过滤。请给 Java 代码。不要先查两次再在内存里 merge。",
      "expected_output": "Uses a same-shaped result type for all branches, then unionAll(...) on entity queryables and downstream filtering on the merged query. Must not fall back to Java Stream concat/merge or raw SQL union strings.",
      "files": []
    },
    {
      "id": 22,
      "name": "whereobject-condition-matrix",
      "prompt": "帮我定义一个 easy-query Java 查询表单 DTO，并给出 whereObject 用法。需求：keyword 同时搜 title/content；publishTime 用一个前端传来的时间区间数组做闭区间筛选；statusList 做 IN；statusAny 做“多个值任意一个相等”而不是 IN；bankCardCode 走 bankCards.code 的 startsWith；说明空字符串、DEFAULT 条件、以及 MULTI_OR 有什么约束。不要退回成手写一长串 where if。",
      "expected_output": "Uses @EasyWhereCondition metadata rather than hand-written gated DSL: MULTI_OR with non-empty propNames, RANGE_CLOSED over an array/list pair, IN, COLLECTION_EQUAL_OR, relation-path propName such as bankCards.code with STARTS_WITH, and explicitly explains that blank strings are skipped unless allowEmptyStrings=true, DEFAULT resolves through DefaultWhereConditionProvider (String -> LIKE/CONTAINS by config, others -> EQUAL), and MULTI_OR property paths must have the same depth.",
      "files": []
    },
    {
      "id": 23,
      "name": "easysearch-vs-whereobject-boundary",
      "prompt": "项目里已经接入了 sql-search。请说明什么时候该用普通 whereObject/@EasyWhereCondition，什么时候该用 EasySearch/@EasyCond；顺便给一个 @EasyCond 查询类例子，要求限制 age 只能 ge/le，createTime 默认排序 desc，并说明 cond 默认是怎么按字段类型推断的。",
      "expected_output": "Explains the boundary: normal DTO form filtering uses whereObject/@EasyWhereCondition, while sql-search parameterized requests use EasySearch/@EasyCond through whereObject(search)+orderByObject(search). Must show condOnly and sort on @EasyCond, and explicitly state source default inference: String -> Like, date/time -> RangeClosed, List -> In, others -> Equals. Should also mention defaultEnabled=false / strict=true behavior from EasySearch options.",
      "files": []
    },
    {
      "id": 24,
      "name": "valueconverter-vs-valueautoconverter",
      "prompt": "帮我给 easy-query 设计一套枚举值映射方案。需求：数据库里存整数 code，Java 实体字段想直接用枚举；同时我不想每个字段都手写 `@Column(conversion=...)`。请说明什么时候用 ValueConverter，什么时候用 ValueAutoConverter，并给出代码。还要说清楚如果字段上已经显式写了 conversion，会不会压过自动转换。",
      "expected_output": "Must distinguish field-level ValueConverter from global ValueAutoConverter, show apply(entityClass, propertyType, property), mention registration through QueryConfiguration.applyValueConverter(...) or Spring bean, explain that auto conversion only participates when the column conversion remains default, and state that explicit @Column(conversion=...) overrides auto conversion. Should not misdescribe JdbcTypeHandler as the primary solution for enum code mapping.",
      "files": []
    },
    {
      "id": 25,
      "name": "jsonb-converter-plus-typehandler",
      "prompt": "PostgreSQL 里我想把 jsonb 列映射成 Java 对象字段，既要查询出来自动反序列化，也要保存时正确写成 PGobject。请说明 easy-query 里 ValueConverter / ValueAutoConverter / TypeHandler 分别负责什么，并给一个合理的组合方案。",
      "expected_output": "Must explain the split: ValueConverter/ValueAutoConverter handle object <-> JSON string/object mapping, while JdbcTypeHandler handles JDBC read/write behavior such as PGobject binding. Should mention @Column(dbType=\"jsonb\", jdbcType=JDBCType.JAVA_OBJECT), runtime registration via applyValueConverter(...) and JdbcTypeHandlerManager.appendHandler(...), and recommend the combined converter + type handler approach for jsonb instead of treating either one as sufficient for both layers.",
      "files": []
    },
    {
      "id": 26,
      "name": "tenant-audit-interceptor-stack",
      "prompt": "帮我给 easy-query 设计一套拦截器方案。需求：BaseEntity 插入时自动填充 createTime/createBy/updateTime/updateBy；对象更新时自动刷新 updateTime/updateBy；表达式更新时如果用户没手写 set 这两个字段，也要自动补上；查询/更新/删除都要自动带 tenantId 条件。请说明分别该用哪些 interceptor 接口，并给 Java 代码。",
      "expected_output": "Must split responsibilities correctly: EntityInterceptor for insert/update object mutation, PredicateFilterInterceptor for tenant where predicates, UpdateSetInterceptor for expression-update set-column autofill, and optionally UpdateEntityColumnInterceptor when entity-update column selection must include audit columns. Must not collapse everything into one hand-written where/set pattern or misdescribe PredicateFilterInterceptor as query-only.",
      "files": []
    },
    {
      "id": 27,
      "name": "protected-interceptor-control",
      "prompt": "项目里有一个多租户拦截器和一个审计拦截器。请解释 easy-query 里的 `useInterceptor()`、`useInterceptor(name)`、`noInterceptor()`、`noInterceptor(name)` 各自会怎么影响当前表达式；另外说明为什么多租户拦截器通常应该实现 ProtectedInterceptor。",
      "expected_output": "Must explain source-backed semantics precisely: defaults already use interceptors, useInterceptor(name) does not mean “only this one”, noInterceptor() disables the default set for the current expression, useInterceptor() re-enables defaults, and ProtectedInterceptor survives global noInterceptor() unless removed by noInterceptor(name). Should recommend noInterceptor().useInterceptor(name) when the user truly wants only one specific interceptor.",
      "files": []
    },
    {
      "id": 28,
      "name": "logic-delete-control-surface",
      "prompt": "帮我说明 easy-query 里的逻辑删除控制面。需求：Company 有 `@LogicDelete(strategy = LogicDeleteStrategyEnum.BOOLEAN)` 的 deleted 字段。我要解释默认删除会发生什么、查询默认会发生什么、什么时候用 `disableLogicDelete()` / `enableLogicDelete()` / `useLogicDelete(false)`，什么时候需要 `tableLogicDelete(() -> false)`，以及物理删除为什么必须再配 `allowDeleteStatement(true)`。",
      "expected_output": "Must explain soft-delete semantics instead of only giving code: delete becomes update, normal queries auto-filter not-deleted rows, disable/enable/useLogicDelete control the current chain, tableLogicDelete is for partial joined-table override rather than global disable, and physical delete requires both disableLogicDelete() and allowDeleteStatement(true). Should not present disableLogicDelete as a default or harmless choice.",
      "files": []
    },
    {
      "id": 29,
      "name": "custom-logic-delete-strategy",
      "prompt": "我想做一个 easy-query 自定义逻辑删除策略：未删除时 deletedTime 为 null，删除时写当前时间，同时额外写 deletedUserId；实体字段上通过 `@LogicDelete(strategy = CUSTOM, strategyName = ...)` 绑定。请给 Java 代码，并说明为什么在 getDeletedSQLExpression 里不能先把 now 提取成局部变量再返回 lambda。顺便说明配置级逻辑删除策略和字段绑定策略有什么区别。",
      "expected_output": "Uses AbstractLogicDeleteStrategy with getPredicateFilterExpression/getDeletedSQLExpression/getStrategy, registration through QueryConfiguration.applyLogicDeleteStrategy(...), field binding through @LogicDelete(strategy = LogicDeleteStrategyEnum.CUSTOM, strategyName = ...), and explicitly explains the delayed-lambda capture pitfall for timestamps/thread-local values. Should also distinguish field-bound custom strategy from AbstractConfigurationLogicDeleteStrategy driven by apply(entityClass).",
      "files": []
    },
    {
      "id": 30,
      "name": "springboot-starter-autoconfig-and-components",
      "prompt": "我们项目用 easy-query 的 Spring Boot starter。请你从源码角度说明三件事：1）starter 到底自动装配了哪些核心 bean；2）`easy-query.enable`、`easy-query.build`、`easy-query-track.enable` 分别控制什么；3）如果我写了 `Interceptor`、`LogicDeleteStrategy`、`ValueConverter`、`JdbcTypeHandler` 这些扩展，哪些只加 `@Component` 就会生效，哪些还要额外处理？不要只贴 yml，要讲清楚自动注册机制。",
      "expected_output": "Must describe the current source topology: `EasyQueryStarterAutoConfiguration`, `EasyQueryStarterBuildAutoConfiguration`, `EasyQueryTrackAopConfiguration`; explain that `easy-query.enable` and `easy-query.build` both use conditional `matchIfMissing=true` semantics so explicit false disables while missing still matches, and that `easy-query-track.enable` controls tracking AOP. Must explain `EasyQueryInitializeOption` bean collection by type, that `Interceptor`/`LogicDeleteStrategy`/`ValueConverter` beans are auto-applied, and that a plain `JdbcTypeHandler` bean is not enough for global binding without `JdbcTypeHandlerReplaceConfigurer`.",
      "files": []
    },
    {
      "id": 31,
      "name": "springboot-multids-custom-build-and-track",
      "prompt": "Spring Boot 里我有两个 DataSource，想给 easy-query 分别建两个 client，还想替换默认的 NameConversion，并且只让某个 service 方法对 `db1` 这个 client 开启 `@EasyQueryTrack`。请你结合源码说清楚默认 starter 为什么不适合直接硬上、多数据源时该怎么处理、`StarterConfigurer` 该放在哪一层、以及 `@EasyQueryTrack(tag=...)` 是按什么匹配的。",
      "expected_output": "Must explain that the default starter build path injects a single `DataSource`, so multiple datasources need either one `@Primary` default client or manual bean construction with `@Qualifier`; should recommend `easy-query.build=false` and custom `EasyQueryClient` / `EasyEntityQuery` beans when multiple clients are needed. Must place `StarterConfigurer` correctly as the service-replacement hook used during `SpringBootStarterBuilder.buildClient(...)`, and explain that `@EasyQueryTrack(tag=\"db1\")` matches `EasyQueryClient` bean names, blank tag means all clients, comma-separated tags select several. Should mention `easy-query-track.enable` / Spring AOP presence as part of the setup boundary.",
      "files": []
    },
    {
      "id": 32,
      "name": "explicit-where-and-select-subquery",
      "prompt": "帮我用 easy-query 写一个 Java 查询：查 Topic，要求存在一个 Blog 满足 `blog.id = topic.id` 且 `blog.title like keyword`，同时结果要返回一个自定义 VO，多一列 `blogCount` 表示该 topic 关联 blog 数量。要求你显式用子查询写，并说明为什么这里优先用 `topic.expression().subQueryable(BlogEntity.class)` 而不是提前单独 `easyEntityQuery.queryable(BlogEntity.class)` 再拼。",
      "expected_output": "Must use explicit correlated subqueries rather than implicit relation sugar: `expression().exists(expression().subQueryable(...).where(...))` in where, plus scalar subquery projection either with `expression().subQuery(...).as(...)` or proxy `setSubQuery(...)` for `blogCount`. Must explicitly explain the context-aware advantage of `expression().subQueryable(...)` for correlated subqueries over an independently built root queryable.",
      "files": []
    },
    {
      "id": 33,
      "name": "derived-table-and-join-subquery",
      "prompt": "给我两个 easy-query Java 例子。第一个：把 SysUser 先投影成一个带 `bankCardCount` 的内嵌视图，再在外层按 `bankCardCount > 1` 过滤并排序。第二个：把 SysBankCard 先按 `bankId` 聚合成发卡数子查询，再 join 回 SysBank。顺便说明什么时候把这个 grouped subquery 升级成 `toCteAs()` 更合适。",
      "expected_output": "First example must model a derived table by continuing on a projected queryable such as `select(...).where(...).orderBy(...)` over `Select.DRAFT` or a proxy VO shape. Second example must build a grouped subquery queryable and `leftJoin` it back to the root, with an explanation that `toCteAs()` is for named/reused/staged SQL pipeline clarity rather than being mandatory for a one-off simple join.",
      "files": []
    },
    {
      "id": 34,
      "name": "object-update-vs-expression-update",
      "prompt": "我已经拿到一个 `User` 实体对象，只想更新它的 `updateTime`，并且条件必须按 `id` 命中。请给 easy-query Java 写法，并说明为什么这里不能只说 `easyEntityQuery.updatable(user).executeRows()`，以及 `setColumns(...)`、`whereColumns(...)`、`setSQLStrategy(...)` 分别在这个场景里解决什么问题。",
      "expected_output": "Must distinguish object update from expression update. For object update it should show something like `easyEntityQuery.updatable(user).setColumns(o -> o.FETCHER.updateTime()).whereColumns(o -> o.FETCHER.id()).executeRows();` and explain that plain `updatable(user).executeRows()` defaults to object-driven full-column update, `setColumns(...)` narrows the SET list, and `whereColumns(...)` keeps the key condition explicit when selected columns are customized. It may additionally mention `setSQLStrategy(ONLY_NOT_NULL_COLUMNS)` when null-overwrite avoidance is the real concern.",
      "files": []
    },
    {
      "id": 35,
      "name": "conflict-write-and-map-update",
      "prompt": "帮我补两个 easy-query Java 片段。第一个：按 `code` 冲突时更新 `updateTime` 和 `title`，不存在则插入，用当前的 upsert API，不要写先查再改。第二个：用 `mapUpdatable` 更新动态表 `user_archive_2024` 的 `update_time`，按 `id` 命中。顺便说明 `onConflictThen(...)` 在冲突列可能为 null 时要注意什么，以及 map 写入的 key 应该写属性名还是列名。",
      "expected_output": "Must use `insertable(...).onConflictThen(...)` rather than check-then-write logic, and for proxy DSL should show a conflict selector like `o -> o.FETCHER.updateTime().title()` plus a constraint selector such as `o -> o.FETCHER.code()`. It must explicitly explain that if the conflict property may be omitted by insert strategy, `setSQLStrategy(SQLExecuteStrategyEnum.ALL_COLUMNS)` may be required. The map example must use `mapUpdatable(map).asTable(\"user_archive_2024\").whereColumns(\"id\")...executeRows()` and explicitly state that map keys are column names, not entity property names.",
      "files": []
    },
    {
      "id": 36,
      "name": "batch-jdbc-semantics",
      "prompt": "请你解释 easy-query 里的 `batch()`、`batch(true)`、`batch(false)` 在写入链上的含义。要求结合源码说清楚它到底是不是底层走 JDBC batch API，以及为什么返回受影响行数不能被当成强业务断言。顺便给一个 insertable 和一个 updatable 的 Java 例子。",
      "expected_output": "Must explicitly state that `batch()` is the default shorthand for `batch(true)`, that the current source toggles batch behavior on the expression and the execution path ultimately uses JDBC `PreparedStatement.addBatch()` / `executeBatch()`, and that `batch(false)` disables that path. It must explain driver-dependent batch counts and why they are unsafe as strong business invariants. Should show both an insert and an update example using `.batch().executeRows()`. Mentioning MySQL `rewriteBatchedStatements=true` as a practical prerequisite is expected.",
      "files": []
    },
    {
      "id": 37,
      "name": "tracking-diff-update-boundary",
      "prompt": "我想让 easy-query 对对象更新走差异更新，不是全字段更新。请你给出 Spring Boot 和纯 Java 两种写法，并说明 `asTracking()`、`addTracking(...)`、`TrackManager.begin()`、`@EasyQueryTrack` 各自的职责边界。顺便解释为什么只写 `asTracking()` 还不够。",
      "expected_output": "Must explain that diff update needs both an active tracking context and a tracked entity. It should show a Spring Boot method using `@EasyQueryTrack` plus `asTracking()` or `addTracking(...)`, and a plain Java example using `TrackManager.begin()` / `release()`. It must distinguish query-side tracking (`asTracking()`), manual entity registration (`addTracking(...)`), and context lifecycle (`TrackManager` / `@EasyQueryTrack`), and explicitly state that `asTracking()` alone is insufficient without an active tracking scope.",
      "files": []
    },
    {
      "id": 38,
      "name": "savable-required-context-and-savepath",
      "prompt": "请你系统说明 easy-query 的 `savable(...)` 使用前提，并给一个安全的 Spring Boot 示例。要求说清楚为什么事务和 tracking 不是可选项，以及 `savePath(...)`、`ignoreRoot()`、`removeRoot()`、`disableLogicDelete()` 各自解决什么问题。不要把它说成普通 insert/update 的替代品。",
      "expected_output": "Must explicitly state that current source requires both an active transaction and a non-null current track context before savable execution, and should show a Spring Boot example using `@Transactional` plus `@EasyQueryTrack`. It must explain `savePath(...)` as a value-object path limiter, `ignoreRoot()` as skipping root save operations, `removeRoot()` as root-removal workflow, and `disableLogicDelete()`/`useLogicDelete(false)` as savable-level logic-delete bypass. It should not describe savable as the default answer for simple row writes.",
      "files": []
    },
    {
      "id": 39,
      "name": "savable-ownership-and-key-safety",
      "prompt": "我有一个一对多保存接口，前端会传子项 id，还有一个对象所属权变更场景：某个值对象要从 A 父对象转到 B 父对象。请你结合 easy-query 源码说明两个问题：1）为什么不能盲信前端子项 id，`saveEntitySetPrimaryKey(...)` 该怎么用；2）为什么默认不允许所有权变更，什么时候才用 `SaveBehaviorEnum.ALLOW_OWNERSHIP_CHANGE`。如果关系是 many-to-one，再顺手说明为什么 `savable(child)` 不会递归保存 parent。",
      "expected_output": "Must explain that request-built child ids are unsafe in savable flows and show a tracked-context-safe use of `easyEntityQuery.saveEntitySetPrimaryKey(child)` so non-tracked child rows get backend-assigned keys. It must explain that ownership reassignment is blocked by default by source save logic and only allowed intentionally through `.configure(s -> s.getSaveBehavior().add(SaveBehaviorEnum.ALLOW_OWNERSHIP_CHANGE))`. It should also explicitly state the many-to-one boundary: `savable(valueObject)` copies aggregate-root relation keys but does not recursively save/detach the aggregate root parent.",
      "files": []
    }
  ]
}

