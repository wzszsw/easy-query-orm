[
  {
    "id": "eq-val-001",
    "task_type": "routing",
    "task_description": "Starter property behavior must not regress",
    "question": "When a Spring Boot user asks whether `easy-query.enable: true` is mandatory, what current-source reference should the skill rely on and what condition behavior should it state?",
    "context": "The answer should use current starter source behavior rather than old docs.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "configuration-starter.md" },
      { "op": "regex", "arg": "(?i)(matchIfMissing|default[s]? to true|enabled by default|property is absent)" },
      { "op": "contains", "arg": "not" }
    ],
    "reference_text": "configuration-starter.md: easy-query.enable uses matchIfMissing=true; missing property still enables starter auto-configuration."
  },
  {
    "id": "eq-val-002",
    "task_type": "boundary",
    "task_description": "Typed SQL versus native SQL boundary should hold on validation",
    "question": "If a user asks for string/date/math/JSON function usage that easy-query already exposes as typed expressions, what should the skill avoid recommending first?",
    "context": "The answer should keep the typed/native split honest.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "typed-sql-expressions.md" },
      { "op": "contains", "arg": "do not" },
      { "op": "contains", "arg": "raw SQL" }
    ],
    "reference_text": "typed-sql-expressions.md is the primary guide; do not drift into raw SQL fragment advice unless the typed surface is truly missing."
  },
  {
    "id": "eq-val-003",
    "task_type": "boundary",
    "task_description": "savable prerequisites should remain intact",
    "question": "What must the skill not describe as optional when explaining `savable(...)` execution?",
    "context": "The skill must preserve the stricter current-source preconditions.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "transaction" },
      { "op": "contains", "arg": "tracking context" },
      { "op": "regex", "arg": "(?i)(not optional|must not be optional|required|requires both)" }
    ],
    "reference_text": "savable-aggregate.md: transaction + tracking context are first-class prerequisites."
  },
  {
    "id": "eq-val-004",
    "task_type": "boundary",
    "task_description": "Tracking explanation should preserve the two-requirement rule",
    "question": "What should the skill say if someone claims `asTracking()` alone enables diff update tracking?",
    "context": "The answer should reject the one-requirement simplification.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "active tracking context" },
      { "op": "contains", "arg": "tracked entity" },
      { "op": "contains", "arg": "asTracking()" }
    ],
    "reference_text": "write-tracking.md: tracking is not enabled just by calling asTracking() alone."
  },
  {
    "id": "eq-val-005",
    "task_type": "boundary",
    "task_description": "Native raw SQL wrapper guidance should remain current-source aligned",
    "question": "Which proxy-side native fragment names should the skill prefer by default, instead of older compatibility forms?",
    "context": "Validation should catch regression to older expression().sql naming.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "rawSQLCommand" },
      { "op": "contains", "arg": "rawSQLStatement" }
    ],
    "reference_text": "native-sql.md and review checks: prefer rawSQLCommand(...) and rawSQLStatement(...)."
  },
  {
    "id": "eq-val-006",
    "task_type": "routing",
    "task_description": "whereObject should stay in the search-form branch",
    "question": "A user asks for a backend/admin page endpoint with optional filters, DTO graph return, and stable sort. Which reference should the skill open before dto-object-query details?",
    "context": "Validation should ensure the combined page/search branch is preserved.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "search-form-page.md" }
    ],
    "reference_text": "search-form-page.md is the first-stop reference for this combined endpoint shape."
  },
  {
    "id": "eq-val-007",
    "task_type": "boundary",
    "task_description": "selectAutoInclude child pruning must stay distinct from root filtering",
    "question": "If the business requirement is to keep the root row but prune rows from a child list, what mechanism should the skill prefer?",
    "context": "Validation should catch answers that misuse root where for child pruning.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "EXTRA_AUTO_INCLUDE_CONFIGURE" },
      { "op": "contains", "arg": "child" }
    ],
    "reference_text": "select-auto-include.md: prefer nested EXTRA_AUTO_INCLUDE_CONFIGURE.where(...) when child rows should be pruned while the root row stays."
  },
  {
    "id": "eq-val-008",
    "task_type": "boundary",
    "task_description": "Spring Boot JDBC handler auto-registration nuance should stay intact",
    "question": "What should the skill say about a plain Spring `JdbcTypeHandler` bean and global auto-binding?",
    "context": "Validation should catch false claims about starter auto-registration.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "JdbcTypeHandlerReplaceConfigurer" },
      { "op": "contains", "arg": "not" },
      { "op": "regex", "arg": "(?i)(plain\\s+spring\\s+`?JdbcTypeHandler`?\\s+bean|plain\\s+`?JdbcTypeHandler`?\\s+bean|JdbcTypeHandler\\s+bean)" }
    ],
    "reference_text": "spring-boot-starter.md and value-conversion-type-handler.md: a plain JdbcTypeHandler bean does not auto-bind globally; mention JdbcTypeHandlerReplaceConfigurer."
  },
  {
    "id": "eq-val-009",
    "task_type": "boundary",
    "task_description": "selectAutoInclude should remain DTO-only and separate-query based",
    "question": "If a user asks whether `selectAutoInclude(Entity.class)` is fine because it is 'just one join anyway', what should the skill preserve?",
    "context": "Validation should catch regression to entity-return or join-based explanations.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "DTO" },
      { "op": "contains", "arg": "not" },
      { "op": "regex", "arg": "(?i)(join|one big join|just one join|single join)" }
    ],
    "reference_text": "select-auto-include.md: use DTO/VO rather than entity, and do not describe the feature as one big join query."
  },
  {
    "id": "eq-val-010",
    "task_type": "boundary",
    "task_description": "Tree recursion filtering must stay split between seed and recursive member",
    "question": "For `asTreeCTE()`, what should the skill say about outer `where(...)` and `setChildFilter(...)`?",
    "context": "Validation should catch tree answers that incorrectly assume outer where applies recursively.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "seed" },
      { "op": "contains", "arg": "setChildFilter" },
      { "op": "contains", "arg": "recursive" }
    ],
    "reference_text": "implicit-query.md: outer where filters only seed rows; setChildFilter(...) controls recursive-member filtering."
  },
  {
    "id": "eq-val-011",
    "task_type": "boundary",
    "task_description": "Local logic-delete bypass should not regress to global disable",
    "question": "If only one joined table should ignore logic delete, what targeted mechanism should the skill prefer?",
    "context": "Validation should catch broad disableLogicDelete guidance for local needs.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "tableLogicDelete" },
      { "op": "regex", "arg": "(?i)(joined table|join target|one joined table|specific joined table)" }
    ],
    "reference_text": "logic-delete.md: use tableLogicDelete(...) when only one joined table should ignore logic-delete filtering."
  },
  {
    "id": "eq-val-012",
    "task_type": "boundary",
    "task_description": "Repeated to-many relation references should keep the group-join recommendation",
    "question": "If one query references the same to-many relation for multiple metrics or predicates, what optimization direction should the skill preserve?",
    "context": "Validation should catch regression to naive repeated correlated-subquery guidance.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "subQueryToGroupJoin" },
      { "op": "contains", "arg": "to-many" }
    ],
    "reference_text": "implicit-query.md: repeated to-many relation usage should steer toward subQueryToGroupJoin(...) or the matching behavior switch."
  },
  {
    "id": "eq-val-013",
    "task_type": "routing",
    "task_description": "Direct review-check questions should be answered without opening references first",
    "question": "If the user asks a question that directly targets a boundary rule already written in this SKILL.md, what should the skill do before reaching for external references?",
    "context": "Validation should catch regressions where the agent opens a reference even though the rule is already in SKILL.md.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "answer directly" },
      { "op": "contains", "arg": "without opening external reference files" }
    ],
    "reference_text": "SKILL.md: directly targeted boundary/review-check questions should be answered from the local rule without opening external references first."
  },
  {
    "id": "eq-val-014",
    "task_type": "boundary",
    "task_description": "Starter condition semantics should stay specific to matchIfMissing",
    "question": "If someone says both `easy-query.enable` and `easy-query.build` must be explicitly set to true or the starter will not initialize, what should the skill preserve?",
    "context": "Validation should catch regression back to vague 'enabled by default' wording without the source-backed condition detail.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "easy-query.enable" },
      { "op": "contains", "arg": "easy-query.build" },
      { "op": "contains", "arg": "matchIfMissing=true" },
      { "op": "contains", "arg": "false" }
    ],
    "reference_text": "configuration-starter.md: both properties use matchIfMissing=true, and only explicit false disables the corresponding branch."
  },
  {
    "id": "eq-val-015",
    "task_type": "boundary",
    "task_description": "One joined table logic-delete bypass should require the table-local API name",
    "question": "A user says 'I only want this one joined table to ignore soft delete; should I just disable logic delete for the query?' What targeted mechanism should the skill preserve?",
    "context": "Validation should catch answers that retreat to broad query-level disable or open a reference instead of answering.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "tableLogicDelete" },
      { "op": "contains", "arg": "joined table" },
      { "op": "contains", "arg": "not" }
    ],
    "reference_text": "logic-delete.md: prefer tableLogicDelete(...) when only one joined table should ignore logic-delete filtering."
  },
  {
    "id": "eq-val-016",
    "task_type": "boundary",
    "task_description": "Validation should keep entity-side conditional navigation separate from DTO child pruning",
    "question": "If the relation path itself should only exist for version-specific roots, what should the skill prefer over nested `EXTRA_AUTO_INCLUDE_CONFIGURE.where(...)` child pruning?",
    "context": "Validation coverage for the new 3.2.13 conditional-navigation feature.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "@IncludeOnProperty" },
      { "op": "contains", "arg": "EXTRA_AUTO_INCLUDE_CONFIGURE" }
    ],
    "reference_text": "entity-modeling-navigate.md and select-auto-include.md: prefer @IncludeOnProperty for static relation gating; EXTRA_AUTO_INCLUDE_CONFIGURE.where(...) is for child pruning."
  },
  {
    "id": "eq-val-017",
    "task_type": "boundary",
    "task_description": "Validation should keep selectAutoInclude aware of entity-side conditional navigation",
    "question": "What should the skill preserve about `selectAutoInclude(...)`, `@IncludeOnProperty`, and whether the root DTO row can still remain?",
    "context": "Validation should catch confusion between root filtering and path activation.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "selectAutoInclude" },
      { "op": "contains", "arg": "@IncludeOnProperty" },
      { "op": "contains", "arg": "root DTO row" }
    ],
    "reference_text": "select-auto-include.md: entity-side @IncludeOnProperty can deactivate the child path while the root DTO row still remains."
  },
  {
    "id": "eq-val-018",
    "task_type": "boundary",
    "task_description": "Validation should keep savable transaction and tracking prerequisites strict",
    "question": "If someone explains `savable(...)` as a generic recursive save without strict preconditions, what two prerequisite categories should the skill insist on?",
    "context": "Validation should catch regression to a permissive savable explanation.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "transaction" },
      { "op": "contains", "arg": "tracking context" }
    ],
    "reference_text": "savable references and review checks: savable(...) requires both transaction and tracking context."
  },
  {
    "id": "eq-val-019",
    "task_type": "boundary",
    "task_description": "Validation should preserve the raw-SQL derived-table entrypoint",
    "question": "If raw SQL is only providing the base derived table and the rest should stay in the DSL, what should the skill prefer instead of jumping straight to full `sqlQuery(...)`?",
    "context": "Validation should catch fallback to the wrong native-SQL entrypoint.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "queryable(rawSql" },
      { "op": "contains", "arg": "sqlQuery" },
      { "op": "contains", "arg": "not" }
    ],
    "reference_text": "native-sql.md and review checks: prefer queryable(rawSql, Entity.class, params) when raw SQL is only the FROM seed."
  },
  {
    "id": "eq-val-020",
    "task_type": "query-shape",
    "task_description": "Validation should keep DTO graph return on selectAutoInclude first",
    "question": "If the service contract is a nested DTO graph rather than entity objects, what result-shape direction should the skill preserve?",
    "context": "Validation should catch drift back to entity include for DTO-first return shapes.",
    "answer_kind": "rule",
    "rule_checks": [
      { "op": "contains", "arg": "selectAutoInclude(ResultDTO.class)" },
      { "op": "contains", "arg": "DTO" }
    ],
    "reference_text": "include-structured-loading.md and select-auto-include.md: DTO/VO graph return should start from selectAutoInclude(ResultDTO.class)."
  }
]
